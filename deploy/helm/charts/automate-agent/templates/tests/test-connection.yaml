apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "automate-it.fullname" . }}-test-connection"
  labels:
    {{- include "automate-it.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "automate-it.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
